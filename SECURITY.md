# سياسة الأمان وقائمة ما قبل النشر — NoorConnect

[![Status](https://img.shields.io/badge/status-قبل%20النشر-orange)](#)

هذا الملف يجمّع كل ما يجب مراجعته وضبطه **قبل** توزيع التطبيق على المستخدمين أو رفعه لأي متجر. لا يُعتبر أي بند هنا اختياريًا للنشر الفعلي.

---

## 1. الالتزام بشروط تليجرام

- [ ] **لا يُستخدم اسم "Telegram" ولا شعاره.** الاسم `NoorConnect` هو placeholder مؤقت — يجب استبداله باسم مميّز فعليًا قبل النشر.
- [ ] إذا استُخدم كود TDLib نفسه (وليس واجهته البرمجية فقط)، فبعض أجزائه قد تخضع لرخصة **GPL** — يجب التحقق من الترخيص الفعلي والالتزام به (بما في ذلك نشر الكود المصدري إن اشترطت الرخصة ذلك).
- [ ] الالتزام بـ [إرشادات تليجرام لأمان البيانات](https://core.telegram.org/api) الخاصة بحماية بيانات المستخدمين.

---

## 2. توقيع إصدار الإنتاج (Signing)

> ⚠️ **حرج**: `signingConfig` الخاص بنسخة `release` مربوط حاليًا بمفتاح الـ **debug** مؤقتًا، لتسهيل قياس حجم الإصدار دون الحاجة لإنشاء keystore حقيقي فورًا.

- [ ] إنشاء keystore حقيقي وربطه بـ `signingConfig` الخاص بـ `release` قبل أي توزيع خارج بيئة الاختبار.
- [ ] عدم رفع أي نسخة موقّعة بمفتاح الـ debug لأي مستخدم نهائي أو متجر.

---

## 3. قواعد أمان Firestore

المشروع يعتمد على مجموعتين رئيسيتين في Firestore:

```
channels/{chatId}
  status:   "whitelist" | "blacklist" | "pending"
  reason:   string, اختياري
  audience: "male" | "female" | "both"

moderation_config/banned_words
  words: array<string>
```

### المتطلبات

- [ ] **يجب** ضبط تبويب "Rules" في Firestore قبل التشغيل الفعلي — بدون ذلك يمكن لأي طرف قراءة/كتابة البيانات بحرية.
- [ ] **لا** تستخدم `allow write: if false` بشكل عام — هذا يكسر خاصية "الإبلاغ التلقائي" التي يحتاجها التطبيق: عندما يكتشف كلمة محظورة أو محادثة جديدة بلا سجل، يحتاج التطبيق صلاحية كتابة `status = pending` تحديدًا.
- [ ] التطبيق (طرف المستخدم) يجب أن يملك صلاحية:
  - قراءة `channels/{chatId}` وكافة الحقول.
  - كتابة `status = "pending"` **فقط** عند عدم وجود سجل أو عند اكتشاف كلمة محظورة — بلا صلاحية تعيين `whitelist` أو `blacklist`.
- [ ] لوحة الإدارة (`admin-panel/`) هي الطرف الوحيد المخوَّل بتغيير `status` إلى `whitelist` أو `blacklist`.
- [ ] **يجب** أن تستخدم لوحة الإدارة والتطبيق **نفس القواعد بالضبط** — أي تعارض بينهما يفتح ثغرة أو يكسر وظيفة الإبلاغ.
- [ ] راجع `admin-panel/README.md` للحصول على نص القواعد النهائي المُعتمد قبل تفعيله في بيئة الإنتاج.

---

## 4. حجم التطبيق وتجريد المكتبات الأصلية

المشكلة الأساسية في الحجم الكبير (لوحظ ~224 ميجا) مصدرها `libtdjni.so` غير مُجرَّدة من رموز التصحيح (unstripped)، ومكرّرة لكل معمارية (arm64-v8a + armeabi-v7a + x86_64) داخل نفس الـ APK. النسخة غير المُجرَّدة من مكتبة C++ كهذه (تضم OpenSSL وغيرها مبنيّة داخلها) قد تصل لـ 150–300 ميجا للمعمارية الواحدة، مقابل 15–40 ميجا بعد التجريد.

مؤشر المشكلة في سجل البناء:
```
Unable to strip the following libraries, packaging them as they are: ...libtdjni.so
```

### الإصلاحات المطبَّقة بالفعل في المشروع

- [x] تجميع معمارية `arm64-v8a` فقط بدلًا من الثلاث معماريات (يقلل حجم المكتبات الأصلية لنحو الثلث).
- [x] تفعيل `isMinifyEnabled` و `isShrinkResources` في `release` (لا يؤثر على `libtdjni.so` نفسها، لكنه يقلل باقي حجم التطبيق).
- [x] إضافة `proguard-rules.pro` مع قاعدة أساسية للحفاظ على كلاسات `org.drinkless.tdlib.*` — الكود الأصلي (native) يستدعيها بالاسم مباشرة عبر JNI، وأي تغيير في التسمية بواسطة R8 سيكسر التطبيق وقت التشغيل لا وقت البناء.

### مطلوب يدويًا (خارج Gradle)

- [ ] تجريد `libtdjni.so` باستخدام `llvm-strip` من NDK الخاص بـ TDLib (المتوفر ضمن `example/android` إذا استُخدم `fetch-sdk.sh`):

```bash
<مسار-NDK>/toolchains/llvm/prebuilt/<host-tag>/bin/llvm-strip --strip-unneeded \
    core/tdlib/src/main/jniLibs/arm64-v8a/libtdjni.so
```

`<host-tag>` يكون مثل `windows-x86_64` أو `linux-x86_64` حسب النظام.

- [ ] قياس الفرق:
```bash
gradlew clean assembleRelease
```
الناتج في `app/build/outputs/apk/release/app-release.apk` — قارن الحجم قبل/بعد التجريد.

---

## 5. القاعدة المعمارية الأمنية للكود

- [ ] التأكد من عدم استيراد `org.drinkless.tdlib.*` خارج `:core:tdlib` و `:data` — أي كسر لهذه القاعدة يزيد سطح المخاطرة عند تحديث/استبدال TDLib مستقبلًا ويصعّب المراجعة الأمنية.

---

## ملخص سريع قبل الضغط على "نشر"

| البند | الحالة |
|---|---|
| اسم/شعار بديل عن Telegram | ⏳ مطلوب |
| مراجعة ترخيص GPL إن استُخدم كود TDLib | ⏳ مطلوب |
| keystore إنتاج حقيقي (بدل debug) | ⏳ حرج |
| قواعد Firestore Security Rules مفعّلة ومطابقة بين التطبيق ولوحة الإدارة | ⏳ مطلوب |
| تجريد `libtdjni.so` يدويًا | ⏳ مُوصى به بقوة |
